#include "monty.h"

/**
 * m_nop - does nothing
 * @stack: pointer to the stack
 * @number:  number of the opcode
 */

void m_nop(stack_t **stack, unsigned int number)
{
	(void) stack;
	(void) number;
}
